from setuptools import setup, find_packages

setup(
    name = "GiTall",
    version = "3.1",
    packages = find_packages(),
    install_requires = [
        'https://github.com/danaars/test_ett'
    ]
)
